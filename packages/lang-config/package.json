{
  "name": "@chaos/lang-config",
  "version": "0.0.0",
  "private": true,
  "description": "Language module interface for ChaosLengua and ChaosLimbă — TypeScript types per-language modules conform to (intonation rules, error taxonomy). Zero runtime dependencies; types-only.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint src",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
